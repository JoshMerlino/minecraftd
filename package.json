{
  "name": "mcd",
  "module": "src/index.ts",
  "version": "1.0.0",
  "entry": "d",
  "bin": {
    "mcd": "index.js"
  },
  "type": "module",
  "scripts": {
    "dev": "bun build src/index.ts --outfile=index.js --target=bun --watch",
    "build": "bun build src/index.ts --outfile=index.js --target=bun --minify",
    "compile": "bun build src/index.ts --compile --outfile=mcd"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}